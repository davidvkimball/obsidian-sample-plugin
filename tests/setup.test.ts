/**
 * Placeholder test — verifies the testing infrastructure works.
 * Replace this file with your own tests once you start adding logic.
 *
 * See tests/README.md for the full testing guide.
 */

import { describe, it, expect } from 'vitest';

describe('test setup', () => {
	it('vitest is configured correctly', () => {
		expect(true).toBe(true);
	});
});
